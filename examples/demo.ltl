// examples/demo.ltl — runnable demo for ltl_boids

import std::io
use ltl_boids as lib

fn main() {
    io::println("ltl_boids demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}