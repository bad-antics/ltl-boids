// ltl-boids/main.ltl
//
// Reynolds' boids flocking simulation.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-boids v0.1.0")
    io::println("Reynolds' boids flocking simulation.")
    0
}
